<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dhis2">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="allowBackup">
        <activity android:name=".usescases.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".usescases.login.LoginActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity android:name=".usescases.main.MainActivity" />

        <activity android:name=".usescases.programDetail.ProgramDetailActivity" />
        <activity android:name=".usescases.searchTrackEntity.SearchTEActivity" />
        <activity android:name=".usescases.trackedEntityEnrollment.EnrollmentActivity" />
        <activity android:name=".usescases.teiDashboard.mobile.TeiDashboardMobileActivity" />
        <activity android:name=".usescases.teiDashboard.tablet.TeiDashboardTabletActivity" />
        <activity android:name=".usescases.programDetailTablet.ProgramDetailTabletActivity" />
        <activity android:name=".usescases.teiDashboard.teiDataDetail.TeiDataDetailActivity" />
        <activity android:name=".usescases.eventDetail.EventDetailActivity" />

        <service
            android:name=".data.service.SyncService"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>
