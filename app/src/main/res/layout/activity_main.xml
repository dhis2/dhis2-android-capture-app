<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="com.dhis2.R" />

        <variable
            name="theme"
            type="android.content.res.Resources.Theme" />

        <variable
            name="userName"
            type="java.lang.String" />

        <variable
            name="presenter"
            type="com.dhis2.usescases.main.MainContracts.Presenter" />

        <variable
            name="currentFragment"
            type="android.databinding.ObservableInt" />
    </data>

    <android.support.v4.widget.DrawerLayout
        android:id="@+id/drawer_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:openDrawer="start">

        <!--Main Content-->

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <LinearLayout
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?android:attr/actionBarSize"
                android:background="?colorPrimary"
                android:gravity="center_vertical">

                <ImageView
                    android:id="@+id/menu"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:onClick="@{()->presenter.onMenuClick()}"
                    android:padding="15dp"
                    app:srcCompat="@drawable/ic_menu"
                    tools:ignore="ContentDescription" />

                <TextView
                    android:id="@+id/title"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:gravity="start"
                    android:text="@string/done_task"
                    android:textColor="@android:color/white"
                    android:textSize="20sp" />

                <ImageView
                    android:id="@+id/filter"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:onClick="@{()->presenter.showFilter()}"
                    android:padding="15dp"
                    app:srcCompat="@drawable/ic_filter_list"
                    tools:ignore="ContentDescription" />

            </LinearLayout>

            <FrameLayout
                android:id="@+id/fragment_container"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_below="@id/toolbar" />
        </RelativeLayout>

        <!--End of Main Content-->

        <android.support.design.widget.NavigationView
            android:id="@+id/nav_view"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:background="?colorPrimary"
            app:itemTextColor="?colorPrimary">

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <TextView
                    android:id="@+id/user_info"
                    android:layout_width="match_parent"
                    android:layout_height="?attr/actionBarSize"
                    android:gravity="center"
                    android:text="@{userName, default=user_name}"
                    android:textColor="@color/white_dfd"
                    android:textSize="21sp" />

                <TextView
                    android:id="@+id/appVersion"
                    android:layout_width="wrap_content"
                    android:layout_height="?attr/actionBarSize"
                    android:layout_alignParentEnd="true"
                    android:layout_marginEnd="10dp"
                    android:gravity="center"
                    android:text="v1.0"
                    android:textColor="@color/white_dfd"
                    android:textSize="16sp" />

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layout_above="@id/block_button"
                    android:layout_below="@id/user_info"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/menu_done_tasks"
                        android:layout_width="match_parent"
                        android:layout_height="50dp"
                        android:gravity="center_vertical"
                        android:onClick="@{()->presenter.changeFragment(R.id.menu_done_tasks)}"
                        android:padding="10dp"
                        android:text="@string/done_task"
                        android:textColor="@color/colorAccent"
                        android:textSize="18sp"
                        app:currentFragment="@{currentFragment}" />

                    <TextView
                        android:id="@+id/sync_manager"
                        android:layout_width="match_parent"
                        android:layout_height="50dp"
                        android:gravity="center_vertical"
                        android:onClick="@{()->presenter.changeFragment(R.id.sync_manager)}"
                        android:padding="10dp"
                        android:text="@string/SYNC_MANAGER"
                        android:textColor="@color/colorAccent"
                        android:textSize="18sp"
                        app:currentFragment="@{currentFragment}" />

                    <TextView
                        android:id="@+id/qr_scan"
                        android:layout_width="match_parent"
                        android:layout_height="50dp"
                        android:gravity="center_vertical"
                        android:onClick="@{()->presenter.changeFragment(R.id.qr_scan)}"
                        android:padding="10dp"
                        android:text="@string/QR_SCANNER"
                        android:textColor="@color/colorAccent"
                        android:textSize="18sp"
                        app:currentFragment="@{currentFragment}" />

                    <TextView
                        android:id="@+id/menu_jira"
                        android:layout_width="match_parent"
                        android:layout_height="50dp"
                        android:gravity="center_vertical"
                        android:onClick="@{()->presenter.changeFragment(R.id.menu_jira)}"
                        android:padding="10dp"
                        android:text="@string/jira_report"
                        android:textColor="@color/colorAccent"
                        android:textSize="18sp"
                        app:currentFragment="@{currentFragment}" />

                    <TextView
                        android:id="@+id/menu_about"
                        android:layout_width="match_parent"
                        android:layout_height="50dp"
                        android:gravity="center_vertical"
                        android:onClick="@{()->presenter.changeFragment(R.id.menu_about)}"
                        android:padding="10dp"
                        android:text="@string/about"
                        android:textColor="@color/colorAccent"
                        android:textSize="18sp"
                        app:currentFragment="@{currentFragment}" />


                </LinearLayout>

                <Button
                    android:id="@+id/logout_button"
                    android:layout_width="match_parent"
                    android:layout_height="48dp"
                    android:layout_alignParentBottom="true"
                    android:layout_gravity="bottom"
                    android:background="?colorPrimaryDark"
                    android:onClick="@{() -> presenter.logOut()}"
                    android:text="@string/log_out"
                    android:textColor="@color/white_faf" />

                <Button
                    android:id="@+id/block_button"
                    android:layout_width="match_parent"
                    android:layout_height="48dp"
                    android:layout_above="@+id/logout_button"
                    android:layout_alignParentStart="true"
                    android:layout_gravity="bottom"
                    android:background="?colorPrimaryDark"
                    android:onClick="onLockClick"
                    android:text="@string/block_session"
                    android:textColor="@color/white_faf" />
            </RelativeLayout>

        </android.support.design.widget.NavigationView>

        <RelativeLayout
            android:id="@+id/pin_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="?colorPrimary"
            android:gravity="center"
            android:visibility="gone">

            <com.andrognito.pinlockview.IndicatorDots
                android:id="@+id/indicator_dots"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:gravity="center" />

            <com.andrognito.pinlockview.PinLockView
                android:id="@+id/pin_lock_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@id/indicator_dots"
                app:indicatorType="fillWithAnimation"
                app:keypadButtonSize="72dp"
                app:keypadDeleteButtonSize="24dp"
                app:keypadHorizontalSpacing="36dp"
                app:keypadShowDeleteButton="true"
                app:keypadTextColor="?colorAccent"
                app:keypadVerticalSpacing="24dp"
                app:pinLength="4" />
        </RelativeLayout>
    </android.support.v4.widget.DrawerLayout>
</layout>