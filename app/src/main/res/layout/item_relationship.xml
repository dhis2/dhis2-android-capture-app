<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="relationship"
            type="org.hisp.dhis.android.core.relationship.RelationshipModel" />

        <variable
            name="presenter"
            type="com.dhis2.usescases.teiDashboard.TeiDashboardContracts.Presenter" />
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginEnd="20dp"
        android:layout_marginStart="20dp"
        android:background="@drawable/gray_border_box"
        android:paddingBottom="19.5dp"
        android:paddingEnd="10dp"
        android:paddingStart="29dp"
        android:paddingTop="19.5dp">

        <TextView
            android:id="@+id/relationShipAttr"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif"
            android:textColor="#787878"
            android:textSize="14.2sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Risk factors:\nHola\nHola" />

        <ImageView
            android:id="@+id/clear_relationship_button"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:onClick="@{ () -> presenter.deleteRelationship(relationship.id)}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:srcCompat="@drawable/ic_clear_red"
            tools:ignore="ContentDescription" />

        <ImageView
            android:id="@+id/tei_relationship_link"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_marginEnd="10dp"
            app:layout_constraintEnd_toStartOf="@id/clear_relationship_button"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:srcCompat="@drawable/ic_insert_link"
            tools:ignore="ContentDescription" />
    </android.support.constraint.ConstraintLayout>
</layout>