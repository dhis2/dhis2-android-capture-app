<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="row"
            type="com.dhis2.usescases.main.trackentitylist.RowHeaderModel"/>

        <variable
            name="presenter"
            type="com.dhis2.usescases.main.trackentitylist.TrackEntityListContractsModule.Presenter" />
    </data>

    <RelativeLayout android:id="@+id/root"
                android:layout_width="55dp"
                android:layout_height="50dp">

    <LinearLayout
        android:id="@+id/row_header_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/table_row_white"
        android:gravity="center_vertical"
        android:orientation="vertical">

        <TextView
            android:id="@+id/row_header_textview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="1"
            android:text="@{row.name}"
            android:textColor="@color/cell_text_color"
            android:textSize="12dp"
            tools:text="Row Data"/>

    </LinearLayout>

    <View
        android:layout_width="1px"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:background="@color/table_divider"/>
    </RelativeLayout>
</layout>